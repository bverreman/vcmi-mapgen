"""The identity-rebuild engine: shape-relative zone templates, bit-exact same-shape replay,
and warp adaptation onto a different shape. ``zone_engine.py`` is the thin CLI on top of this;
import submodules directly, e.g. ``from vcmi_mapgen.rebuild import engine``.
"""
