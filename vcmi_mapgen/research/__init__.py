"""Developer tooling that inspects or scores the generator — not code the generator needs
to run. Each module here also has its own standalone CLI; import submodules directly, e.g.
``from vcmi_mapgen.research import zone_field``.
"""
